import React from "react";
import "./PlayViewWrapper.style.scss";

const PlayViewWrapper = ({ children }) => {
  return <section className="play-view">{children}</section>;
};

export default PlayViewWrapper;
